# Log-Analysis
A FastAPI-based REST API that reads and parses application log files and exposes them through structured endpoints. Supports filtering logs by level, component, and time range, fetching logs by unique ID, and generating summary statistics to help with debugging, monitoring, and log analysis..
